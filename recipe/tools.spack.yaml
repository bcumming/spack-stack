spack:
  include:
  - compilers.yaml
  - config.yaml
  concretizer:
    unify: when_possible
  specs:
  - ccache
  - cmake
  - gmake
  - ninja
  - python@3.10
  - libtree
  - tree
  - squashfs +gzip +lz4 +lzo +xz +zstd
  view: false
  packages:
    all:
      compiler: [gcc]
