spack:
  include:
  - compilers.yaml
  - config.yaml
  concretizer:
    unify: when_possible
  specs:
  - cmake
  - python@3.10
  - libtree
  - tree
  view: false
  packages:
    all:
      compiler: [gcc]
